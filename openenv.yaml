name: customer-support-ai
description: Customer support simulation environment for training AI agents
version: 1.0

action_space:
  type: text
  description: Agent response to customer query

observation_space:
  type: text
  description: Customer query with context (history + sentiment)

reward_range: [0.0, 1.0]
